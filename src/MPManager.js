


let handleComingData = (jsonData) => {
    let comingData = JSON.parse(jsonData);
    if(comingData.type=="statepack") {
        let playersData = data.players;
        let firesData = data.fires;
    } else if(comingData.type=="welcome") {
        
    }

}